query UserAchievements($active:Boolean!){
    userAchievements(data:{active:$active}){
        id
        title
        description
        days
        type
        completed
        published
        picture
        created_at
    }
}